import 'package:flutter/material.dart';

class SmileyInput extends StatelessWidget {
  const SmileyInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Smiley');
  }
}
